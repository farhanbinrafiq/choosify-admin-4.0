{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/v1/catalog/:path*", "destination": "/api/health?catalogPath=:path*" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
