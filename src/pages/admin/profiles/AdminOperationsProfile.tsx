/**
 * @deprecated Admin self-profile now renders through UnifiedProfileShell + ProfileLayout.
 * Kept as a thin re-export so any lingering imports resolve to the universal shell.
 */
export { default } from './UnifiedProfileShell';
