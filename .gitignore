node_modules/
build/
dist/
dist.previous/
coverage/
.DS_Store
*.log
.env*
!.env.example
src/cms-mirror/_reference/

.vercel
.env*
.data/
.local-pg/
# Approved UI reference - local only, never commit/deploy
design-reference/

# Local-only dev server capture, throwaway probes / fixture scripts / debug shots.
# Anything a sprint needs only transiently is named `_tmp_*` and never committed.
.devserver*.log
_tmp_*
**/_tmp_*
scripts/_tmp_*
